# Prototype provenance

Imported on 2026-07-13 from `noise-curator-v3.1.zip`.

- Archive SHA-256: `425603ead023b513238840bdd7d4c50156850ef952620e34c28c12b455332f7a`
- Source Git remote: `https://github.com/tadeo-ctrl/noisev3.git`
- Source branch: `main`
- Source commit: `c7993b4f673e39f5a6d6116d38aa2061ae9ddafa`
- Source commit subject: `Move the role toggle into Security & account`

The archive's complete three-commit Git history was preserved and adopted as the workspace repository. At import time, the archived `main` ref matched `origin/main` exactly.

The archive also contained ignored local-only resources, including original media, React Native reference files, Word documents, and QA utilities. Those resources were retained locally where supplied. `node_modules` was intentionally regenerated from `package-lock.json` instead of copied from the archive.

The pre-existing NOISE algorithm and product notes were retained and added alongside the prototype so future implementation work can use them as internal context.
